import java.util.array;
public class LargestToSmallest{
public static int[] Largest int[]{
   int largest = 0;
   int smallest = 0;
   
   int largest + arr[0];
   for(int count = 0 count < array.length; count++){
   
   
   if(arr(count) = largest){
        smallest = arr(counter);
   }
   }
   
   int number = 0;
   int[] smalllargest = (smallest, largest);
   for(int range = 0 range < smalllargest; range++){
   number++;
   
   
   
   return number;
   }
}
